services:
  backend:
    build: ./backend
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - /opt/smart-crm/media:/data/media
      - ./ai-service:/ai-service
    env_file:
      - .env

